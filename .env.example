# Copia a .env en el VPS (nunca en git):
#   cp .env.example .env && chmod 600 .env
#
# Tras editar la contraseña:
#   bash scripts/sync-site-auth.sh
#   sudo systemctl reload caddy

YEARPLAN_AUTH_USER=carla
YEARPLAN_AUTH_PASSWORD=cambia-esta-clave
